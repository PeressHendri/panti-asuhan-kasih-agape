{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "composer install --optimize-autoloader --no-dev && npm run build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
